# Exact direct dependency envelope for the CPU CI job.
#
# These versions mirror the accepted primary compatibility environment in
# requirements/compatibility.lock.  Keep this file intentionally small: it
# constrains the project and test extras without installing the CUDA/veRL/vLLM
# closure captured by the full compatibility lock.
aiohttp==3.14.1
hydra-core==1.3.4
numpy==2.2.6
omegaconf==2.3.1
pandas==3.0.3
pillow==12.3.0
pytest==9.1.1
ruff==0.15.22
safetensors==0.8.0
tensordict==0.10.0
torch==2.9.0
transformers==4.57.6
